**mini-SWE-agent** 是普林斯顿与斯坦福大学团队（也就是SWE-bench与SWE-agent的原班人马）推出的一个**极简AI编程智能体**。它仅用大约100行Python核心代码，就能在SWE-bench Verified基准测试中解决超过65%的真实软件工程问题。

它的核心设计哲学是：**通过极致简化系统本身，将最大的自由度交还给大语言模型本身。**

### 💡 它为什么能“以小博大”？

相比于前代或其它复杂的AI编程系统，mini-SWE-agent在关键设计上做了一系列“减法”：

*   **唯工具论，化繁为简**：它**不集成任何定制的代码编辑、搜索工具**，也不使用模型复杂的工具调用接口。它唯一依赖的“工具”就是**Bash终端环境**。任何操作，包括文件编辑、代码编译、运行测试，都交给语言模型通过生成Shell命令来完成。
*   **“无状态”的稳定架构**：它的每一步操作都通过`subprocess.run`独立执行，而非维持一个持续运行的Shell会话。这规避了长时运行会话中命令终止判断困难、Shell意外挂起等许多不稳定因素，也使得将其部署在Docker等沙盒环境中变得非常简单。
*   **线性历史，调试友好**：智能体的每一步执行都只是简单地附加到消息历史中，不涉及复杂的状态管理。这让整个过程清晰透明，对调试、微调与强化学习都非常友好。

### 🎯 它的角色与定位

你正在阅读的《GeneralVLA-2》论文，正是通过SWE-bench Verified这样的基准来评估其“受控长期记忆系统”的效果。而**mini-SWE-agent，正是当前在该基准上评估各类模型性能的默认基础框架之一**。

它主要服务于两类场景：
*   **作为研究基线**：由于其极简设计，它非常适合作为实验的基准系统，让研究者能将注意力集中于语言模型本身或其记忆、规划等模块的改进，而非复杂的智能体框架。
*   **作为日常开发工具**：它轻量、可读性高，可以作为命令行工具在本地快速解决编程任务。

### 💎 一句话总结

在AI编程智能体的语境下，**mini-SWE-agent**是一个**用约100行代码实现、性能强大的极简系统**。它通过**放弃专用工具、完全依赖Bash终端**的设计，让智能体本身变得**轻量、稳定、透明**，为前沿语言模型在复杂软件工程任务上提供了一个**高性能的“竞技场”**。



## 相关

- [[SWE-bench-Live]]
- [[Terminal-Bench Challenges]]
- [[OpenHands Software Agent SDK]]
- [[Governed KnowledgeBank——受控长期记忆系统]]
- [[Terminal-Bench]]
